export function ResourceMarkdownHelp() {
  return (
    <p className="text-xs text-muted-foreground">
      Supports headings, bold, italic, strikethrough, links, images, lists, task lists, blockquotes, code, tables, and horizontal rules.
    </p>
  );
}
